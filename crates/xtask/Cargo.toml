[package]
name = "xtask"
version = "0.4.12"
edition = "2021"

[dependencies]
scryer-core = { path = "../scryer-core" }
# The export-template task stages a sentinel project for the viewer build.
tempfile = "3"

[dev-dependencies]
tempfile = "3"
